import { Dashboard } from "@/components/dashboard/Dashboard";

/**
 * Void — Personal Study Sanctuary.
 * The entire experience is one client dashboard; this route stays static.
 */
export default function Home() {
  return <Dashboard />;
}
